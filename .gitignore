# tools settings folders
/**/.*/**/*
!.github/**/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json

# dependencies
node_modules

# build output
build/
dist/

# Hash manifest of the build output. Regenerated by every build and published as a
# CI artifact per run, so committing it would only add churn.

# prisma generated client
src/generated/

# storybook
!/.storybook/**/*
/storybook-static/
/build-storybook.log

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# local env files
.env.local
.env.development.local
.env.test.local
.env.production.local

PORT=7200
DATABASE_URL=postgresql://postgres:changeme@localhost:7205/postgres
VAULT_URL=http://data.encryption.localhost:7200
UI_URL=http://encryption.localhost:7200
ALLOWED_FRAME_ANCESTORS=http://localhost:7201,http://localhost:7202,http://a.product.localhost:7201,http://a.product.localhost:7202,http://b.product.localhost:7201,http://b.product.localhost:7202
OIDC_JWKS_URL=http://localhost:7203/realms/encryption/protocol/openid-connect/certs
OIDC_SERVER_CLIENT_ID=encryption
# OIDC configuration for the encryption interface.
# Injected by the server into the HTML at runtime (no build-time variables).
OIDC_ISSUER=http://localhost:7203/realms/encryption
OIDC_CLIENT_ID=encryption
OIDC_REDIRECT_URI=http://encryption.localhost:7200/auth/callback
MAILER_SMTP_HOST=127.0.0.1
MAILER_SMTP_PORT=7208
MAILER_SMTP_REQUIRE_TLS=false
MAILER_DEFAULT_DOMAIN=encryption.localhost
EMAIL_PRODUCT_URL=http://localhost:7201
